name: Test with skipped tests and custom check name
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_EVENT_NAME: push
      GITHUB_REF: refs/heads/main
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - id: action
        uses: .
        with:
          github_token: ${{ github.token }}
          files: pristine/EnricoMi--publish-unit-test-result-action/v2.24.0/tests/fixtures/junit-with-skipped.xml
          check_name: 'My Custom Test Results'
          check_run: 'false'
          job_summary: 'false'
          comment_mode: 'off'
          json_file: ${{ github.workspace }}/test-results-skipped.json
          compare_to_earlier_commit: 'false'
          fail_on: 'nothing'
          action_fail: 'false'

      - name: Assert JSON output file exists
        run: |
          if [ ! -f "$GITHUB_WORKSPACE/test-results-skipped.json" ]; then
            echo "FAIL: JSON output file not found"
            exit 1
          fi
          echo "PASS: JSON output file exists"

      - name: Assert JSON output content
        run: |
          echo "JSON content:"
          cat "$GITHUB_WORKSPACE/test-results-skipped.json"
          echo "PASS: JSON output produced for test results with skipped tests"

      - name: Assert json step output is non-empty
        env:
          ACTUAL: ${{ steps.action.outputs.json }}
        run: |
          echo "actual json output: $ACTUAL"
          if [ -z "$ACTUAL" ]; then
            echo "FAIL: expected non-empty json output"
            exit 1
          fi
          echo "PASS: json output is non-empty"
